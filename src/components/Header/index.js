import React from "react"
import './style.scss';

const Header = () => {
    return (
        <header id="header">
            <div className="container">
                <h1>Header</h1>
            </div>
        </header>
    );
}

export default Header;
